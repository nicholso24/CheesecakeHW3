CREATE DATABASE CHEESECAKE;
USE CHEESECAKE;
CREATE TABLE ORDERS (
    ORDERID int,
    MONTH CHAR(3),
    DAY int,
    QUANTITY int,
    TOPPING SET("plain", "chocolate", "cherry"), 
    NOTES VARCHAR(512),
);
DESC ORDERS;
SELECT * FROM ORDERS;
SELECT DISTINCT TOPPING FROM ORDERS;
INSERT INTO ORDERS(ORDERID, MONTH, QUANTITY, TOPPING, NOTES) VALUES (1, JAN, 2, "cherry", none);
